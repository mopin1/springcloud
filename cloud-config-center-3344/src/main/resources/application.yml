server:
  port: 3344

spring:
  application:
    name: cloud-config-center
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mopin1/springcloud-config.git
          #搜索目录
          search-paths:
            - springcloud-config
          username: mopin1
          password: qifuxionger00.00
      #读取分支
      label: master

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka